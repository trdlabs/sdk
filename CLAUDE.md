@AGENTS.md

<!-- Инструкции репозитория живут в AGENTS.md одним файлом. Claude Code читает CLAUDE.md и НЕ
     читает AGENTS.md (code.claude.com/docs/en/memory), поэтому связь обязана быть импортом, а
     не отсылкой прозой. Блочные комментарии вырезаются до инъекции — этот текст контекста не
     стоит (проверено пробой 2026-08-21). ВНИМАНИЕ: `gortex init` перезаписывает блоки между
     своими маркерами; что не должно быть затёрто — держать вне их. -->
