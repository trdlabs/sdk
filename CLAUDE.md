@AGENTS.md

<!-- Импорт выше — не украшение. Claude Code читает CLAUDE.md и НЕ читает AGENTS.md
     (code.claude.com/docs/en/memory), поэтому связь между файлами обязана быть импортом,
     а не отсылкой прозой. Инструкции репозитория живут в ОДНОМ файле — AGENTS.md, который
     читают и другие агенты (Codex и пр.).

     Импорт НЕ снижает вес контекста: импортированное грузится при старте наравне. Вес режут
     удаление, .claude/rules/ с paths: и скиллы.

     Свёрнуто 2026-08-20: до этого блок «Ecosystem» и таблица gortex-скиллов лежали в обоих
     файлах дословно.

     ВНИМАНИЕ: `gortex init` перезаписывает блоки между маркерами gortex:*:start/end.
     Всё, что не должно быть затёрто, держать ВНЕ этих маркеров. -->
