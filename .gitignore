node_modules/
dist/
*.tgz
.artifacts/

# Изолированные рабочие деревья агентов (см. scripts/worktree-init.sh).
# Без этих строк созданное дерево показывается как untracked и уезжает в коммит
# первым же `git add -A` — вместе с чужим node_modules.
.worktrees/
.claude/worktrees/
