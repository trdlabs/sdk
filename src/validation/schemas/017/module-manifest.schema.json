{
  "$id": "https://trading-platform/017/module-manifest.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/ModuleManifest",
  "definitions": {
    "ModuleManifest": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "version": {
          "type": "string"
        },
        "kind": {
          "$ref": "#/definitions/ModuleKind"
        },
        "name": {
          "type": "string"
        },
        "summary": {
          "type": "string"
        },
        "rationale": {
          "type": "string"
        },
        "author": {
          "$ref": "#/definitions/Author"
        },
        "source": {
          "type": "string"
        },
        "contractVersion": {
          "type": "string"
        },
        "status": {
          "$ref": "#/definitions/ModuleStatus"
        },
        "lifecycle": {
          "$ref": "#/definitions/StrategyLifecycle",
          "description": "083 E1 (аддитивно) — объявленная ФОРМА стратегии. Отсутствует ⇒ `single_position`: манифест без этого поля описывает ровно ту же стратегию, что и до 083 (SC-008). Валидатор проверяет соответствие объявленной формы набору хуков (`lifecycle_form_invalid`); исполнение формы — зона рантайма (083 E2–E3), не 017."
        },
        "paramsSchema": {
          "type": "object",
          "description": "Произвольная JSON Schema объявленных параметров (FR-034)."
        },
        "params": {
          "type": "object",
          "description": "Payload параметров (валидируется против `paramsSchema`)."
        },
        "capabilities": {
          "$ref": "#/definitions/CapabilityDeclaration"
        },
        "dataNeeds": {
          "$ref": "#/definitions/DataNeedsDeclaration"
        },
        "hooks": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/LifecycleHook"
          }
        },
        "targetStrategyRef": {
          "type": "string",
          "description": "id целевой стратегии (только overlay)."
        },
        "interceptionPoint": {
          "type": "string",
          "description": "Ровно одна точка перехвата (только overlay)."
        }
      },
      "required": [
        "id",
        "version",
        "kind",
        "name",
        "summary",
        "rationale",
        "author",
        "contractVersion",
        "status",
        "paramsSchema",
        "capabilities",
        "dataNeeds",
        "hooks"
      ],
      "additionalProperties": false,
      "description": "JSON-обёртка вокруг кодового модуля (FR-032). НЕ содержит логики стратегии (FR-033). При submit `status` всегда `research_only` (FR-030)."
    },
    "ModuleKind": {
      "type": "string",
      "enum": [
        "strategy",
        "overlay"
      ],
      "description": "Дискриминатор вида модуля."
    },
    "Author": {
      "type": "string",
      "enum": [
        "human",
        "agent"
      ],
      "description": "Происхождение автора — метаданные, НЕ привилегия (FR-030)."
    },
    "ModuleStatus": {
      "type": "string",
      "enum": [
        "research_only",
        "reviewed",
        "promoted"
      ],
      "description": "Статус неизменяемой версии модуля; forward-only автомат (data-model §14)."
    },
    "StrategyLifecycle": {
      "type": "string",
      "enum": [
        "single_position",
        "event_driven"
      ],
      "description": "Объявленная форма стратегии. Дефолт — `single_position`: манифест без поля `lifecycle` описывает ровно ту же стратегию, что и до 083 (back-compat, SC-008)."
    },
    "CapabilityDeclaration": {
      "type": "object",
      "properties": {
        "exchangeDirect": {
          "type": "boolean"
        },
        "brokerDirect": {
          "type": "boolean"
        },
        "filesystem": {
          "type": "boolean"
        },
        "network": {
          "type": "boolean"
        },
        "process": {
          "type": "boolean"
        },
        "env": {
          "type": "boolean"
        },
        "dynamicEval": {
          "type": "boolean"
        },
        "platformSdk": {
          "type": "boolean"
        }
      },
      "additionalProperties": false,
      "description": "Объявленные возможности модуля (FR-018, data-model §13.3). Любая из закрытого набора запрещённых → `forbidden_capability`. Разрешено: только `platformSdk` + read-only контекст.\n\nDisambiguation: ОТДЕЛЬНЫЙ концепт от CapabilityDescriptor биржевого адаптера (src/contracts/execution/capability-descriptor.ts). Разные домены — не переиспользовать."
    },
    "DataNeedsDeclaration": {
      "type": "object",
      "properties": {
        "closedCandlesUpToCurrent": {
          "type": "boolean"
        },
        "asOfIndicators": {
          "type": "boolean"
        },
        "openInterest": {
          "type": "boolean"
        },
        "liquidations": {
          "type": "boolean"
        },
        "funding": {
          "type": "boolean"
        },
        "taker": {
          "type": "boolean"
        },
        "forwardBars": {
          "type": "boolean"
        },
        "forwardWindow": {
          "type": "boolean"
        },
        "oracle": {
          "type": "boolean"
        },
        "labeling": {
          "type": "boolean"
        },
        "postTradeOutcome": {
          "type": "boolean"
        },
        "wallClock": {
          "type": "boolean"
        },
        "uncontrolledRandom": {
          "type": "boolean"
        }
      },
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Объявленная потребность в данных (FR-012, data-model §13.4). forward/oracle/labeling/postTradeOutcome → `lookahead_violation`; wallClock/uncontrolledRandom → `nondeterminism_violation`.\n\n023 (аддитивно): `openInterest`/`liquidations` — легитимные point-in-time рыночные потребности (каталог `SUPPORTED_MARKET_DATA_KINDS`, catalogs.ts). ДЕКЛАРАТИВНЫЙ контракт ожиданий, НЕ permission gate: выставление `ctx.market` следует составу `MarketTape`, не декларации (FR-010). Любой иной объявленный-`true` флаг вне замкнутого объединения каталогов → `unsupported_market_data_kind`."
    },
    "LifecycleHook": {
      "type": "string",
      "enum": [
        "init",
        "onBarClose",
        "onPositionBar",
        "onPendingIntentBar",
        "dispose",
        "apply",
        "onEvent"
      ],
      "description": "Закрытый набор lifecycle-хуков, курируемый платформой (FR-004, data-model §5).\n\n083 E1 (аддитивно): `onEvent` — единственная точка входа формы `event_driven`. Добавлен В КОНЕЦ (порядок существующих хуков не сдвигается — байт-идентичность нормализованных манифестов)."
    }
  },
  "title": "ModuleManifest"
}
