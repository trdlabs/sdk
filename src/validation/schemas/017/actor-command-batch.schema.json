{
  "$id": "https://trading-platform/017/actor-command-batch.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/ActorCommandBatch",
  "definitions": {
    "ActorCommandBatch": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/ActorCommand"
      },
      "description": "Батч команд — то, что актор возвращает из одного `onEvent` и что пересекает JSON-границу изолята. Хост валидирует именно ЕГО (схема `actor-command-batch`): единичная команда — деталь внутри батча, отдельно через границу не ходит.\n\n**Fail-closed при отказе — задача 6, §3.8.4/§3.10. Два класса отказов разведены, а не смешаны в один:**\n\n| класс | исход | | --- | --- | | штатный risk / domain rejection | prefix committed, suffix skipped, инстанс продолжает работу | | throw из `dispatch`, невалидный по схеме батч, breach бюджета (`ActorBudgets`, ниже) | `halt+finalize` |\n\nФормулировка первого класса дословно (§3.8.4): если валидная по схеме команда получает штатный domain/risk rejection, ранее успешно применённый префикс батча **не откатывается**, отклонённая команда **не имеет частичных эффектов**, а оставшийся суффикс **не применяется**. Соответствующее `order.rejected` / `order.denied` / `cancel.rejected` ставится в очередь и доставляется актору ПОСЛЕ завершения батча (не синхронно внутри текущего `dispatch`). Причина обрыва суффикса — fail-closed: команды ПОСЛЕ отклонённой вычислены под предположением, которое только что опровергнуто (например, доступный notional после отклонённого `place`), и применять их дальше значило бы исполнять план против уже устаревшего состояния.\n\nОтсутствие отката — не упрощение реализации, а физика: откатить уже отправленный в live ордер невозможно, а разное поведение backtest/live на этом шве сломало бы Л4 (cross-host parity).\n\nВторой класс (`throw` из `dispatch`, батч, не прошедший схему на границе, breach любого бюджета из `ActorBudgets`) — авария ядра, а не доменный факт: инстанс переводится в `halt+finalize` целиком, потому что живая позиция при мёртвой стратегии — та же рассинхронизация, что закрывает инвариант «оба состояния умирают вместе» (`mem883ede17e9bfeb24`). Halt наблюдаем актором (может успеть получить `dispose`, если хост его вызывает как часть finalize) — сам детектор breach и его проводка в `halt+finalize` реализуются в S2 (`@trdlabs/engine`); здесь — только форма и наблюдаемое разведение двух классов."
    },
    "ActorCommand": {
      "anyOf": [
        {
          "$ref": "#/definitions/ActorPlaceCommand"
        },
        {
          "$ref": "#/definitions/ActorCancelCommand"
        },
        {
          "$ref": "#/definitions/ActorTimerSetCommand"
        },
        {
          "$ref": "#/definitions/ActorTimerCancelCommand"
        },
        {
          "$ref": "#/definitions/ActorAnnotateCommand"
        }
      ],
      "description": "Замкнутый union команд актора."
    },
    "ActorPlaceCommand": {
      "anyOf": [
        {
          "$ref": "#/definitions/ActorPlaceMarketCommand"
        },
        {
          "$ref": "#/definitions/ActorPlaceLimitCommand"
        },
        {
          "$ref": "#/definitions/ActorPlaceStopMarketCommand"
        }
      ],
      "description": "Подать заявку. `clientOrderId` — свой, детерминированный (от seed/счётчика); повтор уже живого ID — ошибка хоста, а не молчаливая замена. `qtyUsd` — ПРОСЬБА: RiskEngine клампит или отказывает (`order.denied`)."
    },
    "ActorPlaceMarketCommand": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "place"
        },
        "type": {
          "type": "string",
          "const": "market"
        },
        "clientOrderId": {
          "type": "string"
        },
        "side": {
          "$ref": "#/definitions/OrderSide"
        },
        "qtyUsd": {
          "type": "number",
          "description": "Запрашиваемый нотионал в USD (до клампа риском).",
          "exclusiveMinimum": 0
        },
        "tif": {
          "$ref": "#/definitions/TimeInForce"
        },
        "reduceOnly": {
          "type": "boolean",
          "description": "Заявка только уменьшает экспозицию; проходит и в состоянии REDUCING."
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "rationale": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "type",
        "clientOrderId",
        "side",
        "qtyUsd"
      ],
      "additionalProperties": false,
      "description": "Рыночная заявка: исполняется по состоянию среды, цены не несёт."
    },
    "OrderSide": {
      "type": "string",
      "enum": [
        "buy",
        "sell"
      ],
      "description": "Сторона заявки. Отдельно от `'long' | 'short'` решений 017: заявка — buy/sell, не позиция."
    },
    "TimeInForce": {
      "type": "string",
      "enum": [
        "gtc",
        "ioc"
      ],
      "description": "Time-in-force заявки (вокабуляр 083 `ActorCommand.tif`)."
    },
    "ActorPlaceLimitCommand": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "place"
        },
        "type": {
          "type": "string",
          "const": "limit"
        },
        "clientOrderId": {
          "type": "string"
        },
        "side": {
          "$ref": "#/definitions/OrderSide"
        },
        "qtyUsd": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "price": {
          "type": "number",
          "description": "Лимитная цена."
        },
        "tif": {
          "$ref": "#/definitions/TimeInForce"
        },
        "reduceOnly": {
          "type": "boolean"
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "rationale": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "type",
        "clientOrderId",
        "side",
        "qtyUsd",
        "price"
      ],
      "additionalProperties": false,
      "description": "Лимитная заявка: `price` обязателен, триггера нет."
    },
    "ActorPlaceStopMarketCommand": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "place"
        },
        "type": {
          "type": "string",
          "const": "stop_market"
        },
        "clientOrderId": {
          "type": "string"
        },
        "side": {
          "$ref": "#/definitions/OrderSide"
        },
        "qtyUsd": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "stopPrice": {
          "type": "number",
          "description": "Триггерная цена."
        },
        "tif": {
          "$ref": "#/definitions/TimeInForce"
        },
        "reduceOnly": {
          "type": "boolean"
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "rationale": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "type",
        "clientOrderId",
        "side",
        "qtyUsd",
        "stopPrice"
      ],
      "additionalProperties": false,
      "description": "Стоп-маркет заявка: `stopPrice` обязателен, лимитной цены нет."
    },
    "ActorCancelCommand": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "cancel"
        },
        "clientOrderId": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "clientOrderId"
      ],
      "additionalProperties": false,
      "description": "Отменить свою заявку по её `clientOrderId`. `modify` в v1 нет — place-after-cancel (Q3)."
    },
    "ActorTimerSetCommand": {
      "anyOf": [
        {
          "$ref": "#/definitions/ActorTimerSetAtCommand"
        },
        {
          "$ref": "#/definitions/ActorTimerSetAfterCommand"
        }
      ],
      "description": "Поставить таймер: ЛИБО абсолютный `atTs`, ЛИБО относительный `afterUs` — строго одно из двух. Ни то ни другое (когда будить?) и оба сразу (какое из них истина?) — неоднозначные команды, и обе формы закрыты на уровне схемы, а не соглашением."
    },
    "ActorTimerSetAtCommand": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "timer.set"
        },
        "timerId": {
          "type": "string"
        },
        "atTs": {
          "$ref": "#/definitions/TimestampUs",
          "description": "Абсолютный business_ts срабатывания (часы данных, не wall-clock)."
        }
      },
      "required": [
        "kind",
        "timerId",
        "atTs"
      ],
      "additionalProperties": false,
      "description": "Таймер на абсолютный business_ts."
    },
    "TimestampUs": {
      "type": "integer",
      "description": "Момент времени в микросекундах эпохи. Целое, неотрицательное, safe-integer.",
      "minimum": 0
    },
    "ActorTimerSetAfterCommand": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "timer.set"
        },
        "timerId": {
          "type": "string"
        },
        "afterUs": {
          "$ref": "#/definitions/DurationUs",
          "description": "Смещение от `ActorEnvelope.eventTsUs` события, породившего команду, — то есть от frontier `T` его доставки (было «от `ts` события», пока у событий был собственный `ts`; Б-5 его снёс, и единственная база отсчёта теперь одна — конверт). Срок `dueTs = eventTsUs + afterUs` материализуется по правилу §3.8.5, см. `ActorTimerFiredEvent`."
        }
      },
      "required": [
        "kind",
        "timerId",
        "afterUs"
      ],
      "additionalProperties": false,
      "description": "Таймер через `afterUs` от `eventTsUs` обрабатываемого события."
    },
    "DurationUs": {
      "type": "integer",
      "description": "Длительность в микросекундах. Целое; знак допустим (разность моментов)."
    },
    "ActorTimerCancelCommand": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "timer.cancel"
        },
        "timerId": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "timerId"
      ],
      "additionalProperties": false,
      "description": "Снять ранее поставленный таймер."
    },
    "ActorAnnotateCommand": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "annotate"
        },
        "note": {
          "type": "string"
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "kind",
        "note"
      ],
      "additionalProperties": false,
      "description": "Только метаданные в трассу — без действия (аналог `AnnotateDecision` в 017)."
    }
  },
  "title": "ActorCommandBatch"
}
